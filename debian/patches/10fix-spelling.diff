Index: checkinstall/checkinstall
===================================================================
--- checkinstall.orig/checkinstall	2008-06-25 19:47:26.000000000 -0400
+++ checkinstall/checkinstall	2008-06-25 19:47:27.000000000 -0400
@@ -1643,9 +1643,9 @@
 	if [ $STRIP_ELF -eq 1 ]; then
 	   echo
 	   if [ $STRIP_SO_ELF -eq 1 ]; then
-	       echogn "Striping ELF binaries and libraries..."
+	       echogn "Stripping ELF binaries and libraries..."
 	   else
-	       echogn "Striping ELF binaries..."
+	       echogn "Stripping ELF binaries..."
 	   fi
 	   for f in `find ${BUILD_DIR} -type f \( -perm -0100 -or -perm -0010 -or -perm -0001 \)` ; do
 
@@ -1661,7 +1661,7 @@
 	   okfail
 	fi
 				     ###################
-				     # End of striping #
+				     # End of stripping #
 	##############################################################################
 
 
